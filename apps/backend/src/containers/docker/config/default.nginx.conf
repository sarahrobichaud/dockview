server {
    listen 80;
    
    location /instance/ {
        rewrite ^/instance/(.*) /$1 break;
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
    
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
    }
} 